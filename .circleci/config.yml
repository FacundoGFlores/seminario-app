version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.10.1 # the primary container, where your job's commands are run

    steps:
      - checkout # check out the code in the project directory
      - run:
          name: "Publish site"
          command: |
            ssh connan@138.197.207.195 "cd seminario-app && git checkout master && git pull --rebase && docker-compose up -d" 
