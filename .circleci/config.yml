version: 2.1
jobs:
  build:
    docker:
      - image: arvindr226/alpine-ssh

    steps:
      - checkout
      - run:
          name: "Publish site"
          command: |
            ssh -oStrictHostKeyChecking=no connan@138.197.207.195 "cd seminario-app && git checkout master && git pull --rebase && docker-compose up -d" 
